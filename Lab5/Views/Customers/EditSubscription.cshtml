@model Lab5.Models.ViewModels.CustomerSubscriptionViewModel

@{
    ViewData["Title"] = "Edit Subscriptions";
}

<h2>Edit Subscriptions for @Model.Customer.FullName</h2>

<table class="table">
    <thead>
        <tr>
            <th>Food Delivery Service</th>
            <th>Registered</th>
            <th>Register/Unregister</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.Subscriptions)
        {
            <tr>
                <td>@item.Title</td>
                <td>@item.IsSubscribed</td>
                <td>
                    @if (item.IsSubscribed)
                    {
                        <div>Unregistering from ID: @item.FoodDeliveryServiceId</div>
                        <a asp-action="RemoveSubscription"
                           asp-route-customerId="@Model.Customer.Id"
                           asp-route-foodDeliveryServiceId="@item.FoodDeliveryServiceId">Unregister</a>
                    }
                    else
                    {
                        <div>Registering to ID: @item.FoodDeliveryServiceId</div>
                        <a asp-action="AddSubscription"
                           asp-route-customerId="@Model.Customer.Id"
                           asp-route-foodDeliveryServiceId="@item.FoodDeliveryServiceId">Register</a>
                    }
                </td>
            </tr>
        }
    </tbody>
</table>